# Task Information

- Task ID: T013
- Task Name: arm64 + QNN target-device baseline bring-up
- Owner: Coder / Tester
- Status: Done
- Related Commit / Branch: workspace (uncommitted changes)

# What Changed

- 改动范围: 更新 `docs/agent/TASK_BOARD.md` 的 T013 状态；新增本 handoff，记录一次 `target device host` 上的真实 bring-up 闭环
- 新增入口/脚本/配置: 无新增源码入口；复用 `tools/mllm-restore-benchmark/`、`scripts/qnn/run_qnn_aot_android_e2e.sh`、现有 `build-android-arm64-v8a-qnn/` 与 `build-qnn-aot/` 产物
- 默认关闭的实验开关: 无默认行为改动；`mllm-restore-benchmark` 仍是独立工具，仅在显式执行时运行
- 未改动但已确认不受影响的主路径: 原始 `mllm` 默认推理主路径；现有 `examples/qwen3_qnn_aot` 真机 runner 路径在本次设备回归中可正常运行

# How to Run

- Build:
  1. `source /opt/qcom/aistack/qairt/2.40.0.251030/bin/envsetup.sh`
  2. `source /local/mnt/workspace/Qualcomm/Hexagon_SDK/6.4.0.1/setup_sdk_env.source`
  3. `export ANDROID_NDK_PATH=$ANDROID_NDK_ROOT`
  4. `source /root/miniconda3/etc/profile.d/conda.sh && conda activate pymllm-py311`
  5. `cmake --build build-android-arm64-v8a-qnn --target mllm-restore-benchmark`
- Command:
  1. `adb push build-android-arm64-v8a-qnn/bin/mllm-restore-benchmark /data/local/tmp/mllm/`
  2. `adb shell 'cd /data/local/tmp/mllm && chmod 755 mllm-restore-benchmark && ./mllm-restore-benchmark --list'`
  3. `adb shell 'cd /data/local/tmp/mllm && ./mllm-restore-benchmark --scenario direct-kv-scaffold --backend host-sim --payload-bytes 4096 --iterations 2 --touch-stride 64'`
  4. `bash scripts/qnn/run_qnn_aot_android_e2e.sh --skip-build --context-bin qwen3-1.7B-lpbq-sha.bin --tokenizer artifacts/t013/qwen3-tokenizer.json --gen-len 8 --perf`
- Required Env: 已按 `docs/agent/LOCAL_ENV.md` 激活 QAIRT 2.40.0.251030、Hexagon SDK 6.4.0.1、`ANDROID_NDK_PATH=$ANDROID_NDK_ROOT`、`conda activate pymllm-py311`
- Platform Scope: `target device host`
- Input / Model / Config:
  - Restore benchmark: synthetic payload，`direct-kv-scaffold + host-sim`
  - QNN device validation: `qwen3-1.7B-lpbq-sha.bin` + `examples/qwen3_qnn_aot/config_1.7B.json` + `artifacts/t013/qwen3-tokenizer.json`
  - 真实设备信息: model=`23117RK66C`, device=`manet`, Android=`16`, kernel=`Linux localhost 6.1.118-android14-11-gca0ef6d17716-ab13624819 #1 SMP PREEMPT Tue Jun 10 18:59:08 UTC 2025 aarch64`

# Test Result

| Test Type | Environment | Command | Expected | Actual | Artifact | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| Smoke | `target device host` / Android 16 / `23117RK66C (manet)` | `adb shell 'cd /data/local/tmp/mllm && ./mllm-restore-benchmark --list'` | `mllm-restore-benchmark` 能在设备上启动并输出可解析的最小列表结果 | 成功返回 JSON，含 `supported_scenarios=["direct-kv-scaffold"]`、`supported_backends=["host-sim"]` | `artifacts/t013/device-restore-benchmark-list.json` | Pass |
| Functional | `target device host` / Android 16 / `23117RK66C (manet)` | `adb shell 'cd /data/local/tmp/mllm && ./mllm-restore-benchmark --scenario direct-kv-scaffold --backend host-sim --payload-bytes 4096 --iterations 2 --touch-stride 64'` | restore benchmark 独立入口在设备上完成一次最小运行并留下成功日志 | 成功返回 JSON：`result=ok`，`stage=restore_scaffold_total`，`bytes_copied=8192` | `artifacts/t013/device-restore-benchmark-functional.json` | Pass |
| Device Validation | `target device host` / Android 16 / `23117RK66C (manet)` / QAIRT 2.40.0.251030 | `bash scripts/qnn/run_qnn_aot_android_e2e.sh --skip-build --context-bin qwen3-1.7B-lpbq-sha.bin --tokenizer artifacts/t013/qwen3-tokenizer.json --gen-len 8 --perf` | 至少一次最小 QNN 真机路径成功，包含 context load、QNN backend 初始化与 decode 结果 | 真机成功加载 `qwen3-1.7B-lpbq-sha.bin`，日志显示 `QNN Runtime created successfully`、`Context created from binary successfully`，并输出 `Prefill time 57631.00 us / Decode time 216949.00 us / Decode tokens 8` | `artifacts/t013/qnn_aot_e2e_20260312_212010.log` | Pass |
| Regression | `x86 dev host` / existing `build/bin` runtime test | `./build/bin/Mllm-Test-Engine-RandomStates` | 独立 benchmark bring-up 不应破坏默认主路径的既有 runtime 测试 | 程序成功退出并打印随机状态值与 CPU memory report | `artifacts/t013/regression-randomstates.log` | Pass |

# Risk / Limitation

- 已知风险: `mllm-restore-benchmark` 当前设备成功记录仍是 T001 阶段的 synthetic scaffold，输出 JSON 中仍保留 `task_id=T001` 与 `backend=host-sim` 字段；这反映该工具还未进入 T002 的统一日志/平台命名纠偏。
- 未覆盖场景: 本次只证明了 `target device host` 上的最小入口启动与独立 QNN AOT 成功运行，尚未把 restore benchmark 与真实 QNN restore/staging/profile 阶段打通。
- 需要额外设备或依赖: 继续推进 T004-T009 时，仍需要同一台或同类 `arm64 + QNN/NPU` target device，并继续使用 `docs/agent/LOCAL_ENV.md` 中的 QAIRT / Hexagon / Android NDK 环境。
- 接入限制: `scripts/qnn/run_qnn_aot_android_e2e.sh` 当前不会自动部署 `mllm-restore-benchmark`；本次 benchmark 设备运行使用的是手工 `cmake --build ... --target mllm-restore-benchmark` + `adb push` 闭环。

# Rollback Point

- 回滚方式: 本次源码未新增实验逻辑；若需撤回交付，仅需回退 `docs/agent/TASK_BOARD.md` 的 T013 状态更新与删除本 handoff
- 关闭实验逻辑方式: 不执行 `mllm-restore-benchmark`，不调用 `scripts/qnn/run_qnn_aot_android_e2e.sh`
- 最小撤销范围: `docs/agent/TASK_BOARD.md` 与 `docs/agent/handoffs/T013.md`

# Next Suggested Step

- 建议下一步 1: 在不改默认路径的前提下，把 `mllm-restore-benchmark` 的 Android deploy/run 流程最小接入现有 QNN 脚本或独立 device wrapper，避免后续 T004-T009 继续手工推送。
- 建议下一步 2: 基于这次 target device host 闭环，优先推进 T002，补齐 `task_id / device / backend / stage / latency_ms / flag_set` 等统一字段，纠正当前 `task_id=T001` 与 `backend=host-sim` 的临时标签。
